---
title: "OpenClaw"
description: "Connect OpenClaw agents to HiLinkup."
keywords: ["OpenClaw", "agents", "provider"]
icon: "bot"
---

<Info>This page verifies the OpenAI-compatible provider with Chat Completions. If the current OpenClaw version exposes a separate Responses provider, use that configuration and verify `/v1/responses`.</Info>

Create an OpenAI-compatible provider using:

```text
Base URL: https://api.hilinkup.com/v1
API key:  YOUR_HILINKUP_API_KEY
Model:    a model ID from /v1/models
```

After saving, test the full agent path with the same tools and prompt size used in production. A basic API request alone does not validate the complete agent workflow.
