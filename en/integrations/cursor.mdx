---
title: "Cursor"
description: "Use HiLinkup models in Cursor."
keywords: ["Cursor", "custom API", "OpenAI"]
icon: "mouse-pointer-2"
---

<Info>This page describes the traditional OpenAI-compatible setup, which normally uses Chat Completions. If the current Cursor version explicitly offers Responses, select that mode and verify `/v1/responses`.</Info>

In Cursor model settings, add an OpenAI-compatible provider:

- API key: your HiLinkup API key
- Base URL: `https://hilinkup.com/v1`
- Model: an ID from `/v1/models`

Test with a short chat request. If the model is rejected, verify the exact ID and account access.
